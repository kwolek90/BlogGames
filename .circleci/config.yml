
version: 2.0

jobs:
  build:
    docker:
    - image: circleci/node:stretch
    steps:
    - checkout
    - run: "scp -o StrictHostKeyChecking=no -r circle-pushing/* root@kwolek.nazwa.pl:/var/www/html/games/"
workflows:
  version: 2
  build:
    jobs:
    - build